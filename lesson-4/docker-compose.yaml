services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      APP_STORE: ${APP_STORE}
      APP_REDIS_URL: ${APP_REDIS_URL}
    depends_on:
      redis:
        condition: service_started

  redis:
    image: redis:7-alpine
